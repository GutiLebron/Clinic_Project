projection implementation in class ZBP_C_CLINIC_SPEC unique;
strict ( 2 );
extensible;
use draft;
use side effects;
define behavior for ZC_CLINIC_SPEC alias Spec
extensible
use etag
{
  use create;
  use update;
  //use delete; Borrado lógico
  use action DeactivateSpec;
  use action ReactivateSpec;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

}